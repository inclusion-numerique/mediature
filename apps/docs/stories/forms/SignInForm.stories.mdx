import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { SignInForm } from '@mediature/main/app/(visitor-only)/auth/sign-in/SignInForm';
import { SignInPrefillSchema } from '@mediature/main/models/actions/auth';

<Meta title="Forms/SignIn" component={SignInForm} />

# SignInForm

## Props

<ArgsTable of={SignInForm} />

## Examples

<Canvas>
  <Story name="Empty" parameters={commonParameters}>
    <SignInForm />
  </Story>
</Canvas>

<Canvas>
  <Story name="Filled" parameters={commonParameters}>
    <SignInForm
      prefill={SignInPrefillSchema.parse({
        email: 'jean@france.fr',
        password: 'mypassword',
        rememberMe: true,
      })}
    />
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Logged out"
    parameters={{
      ...commonParameters,
      nextjs: {
        navigation: {
          query: {
            session_end: true,
          },
        },
      },
    }}
  >
    <SignInForm />
  </Story>
</Canvas>

export const commonParameters = {};
