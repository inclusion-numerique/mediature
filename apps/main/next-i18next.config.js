module.exports = {
  // For it's not `next-i18next` is not compatible with app directory so we fallback using `react-i18next` on the client side
  i18n: {
    locales: ['fr'],
    defaultLocale: 'fr',
  },
};
