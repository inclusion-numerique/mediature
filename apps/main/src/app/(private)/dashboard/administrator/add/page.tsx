'use client';

import { AddAdminPage, AddAdminPageProps } from '@mediature/main/src/app/(private)/dashboard/administrator/add/AddAdminPage';

export default function Page(props: AddAdminPageProps) {
  return <AddAdminPage {...props} />;
}
