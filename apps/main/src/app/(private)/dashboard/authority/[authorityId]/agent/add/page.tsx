'use client';

import { AddAgentPage, AddAgentPageProps } from '@mediature/main/src/app/(private)/dashboard/authority/[authorityId]/agent/add/AddAgentPage';

export default function Page(props: AddAgentPageProps) {
  return <AddAgentPage {...props} />;
}
