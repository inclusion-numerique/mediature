'use client';

import { CasePage, CasePageProps } from '@mediature/main/src/app/(private)/dashboard/authority/[authorityId]/case/[caseId]/CasePage';

export default function Page(props: CasePageProps) {
  return <CasePage {...props} />;
}
