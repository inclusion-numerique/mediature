'use client';

import { MetricsPage, MetricsPageProps } from '@mediature/main/src/app/(private)/dashboard/metrics/MetricsPage';

export default function Page(props: MetricsPageProps) {
  return <MetricsPage {...props} />;
}
