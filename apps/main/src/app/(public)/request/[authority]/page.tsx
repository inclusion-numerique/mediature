'use client';

import { RequestCasePage, RequestCasePageProps } from '@mediature/main/src/app/(public)/request/[authority]/RequestCasePage';

export default function Page(props: RequestCasePageProps) {
  return <RequestCasePage {...props} />;
}
